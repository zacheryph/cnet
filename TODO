* socket stats
* cleanup _bind/_connect so you dont need to getsockname();
* make block/nonblock an option? (do i really want this)
* acl for listening sockets? (bloat?)
* on_written callback?
* fix cnet_write to only malloc/memcpy when needed.
* check return on system calls (socket)
* use unsigned where called for
* use sprintf for itoa (faster)
* rewrite on_newclient to accept so many at a time (prevent ddos/socket starvation)
* rewrite on_readable (to long, and linemode possibly completely busted, starvation)